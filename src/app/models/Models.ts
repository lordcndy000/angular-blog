export interface SignInModel {
  token: string;
  user: any;
}

export interface ToDo {
  title: String;
  start: any;
  due: any;
  notes: String;
  priority: Boolean;
  starred: Boolean;
  done: Boolean;
  by: anu;
}
